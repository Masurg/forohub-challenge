spring.application.name=forohub

# Conexión a PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/forohub_db
spring.datasource.username=postgres
spring.datasource.password=1598753zxcbnmM

# Ver el SQL que hace Hibernate en la consola
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configuración de Flyway para que no falle si la base está vacía
spring.flyway.baseline-on-migrate=true